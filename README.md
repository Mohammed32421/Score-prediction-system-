# Score-prediction-system-
Built an ML system predicting student math scores. Used data preprocessing, hyperparameter tuning (RandomizedSearchCV), compared 7 models, achieved R²>0.88. Deployed via Flask web app + JSON API with logging and error handling.
